import { SectionPlaceholder } from "@/components/admin/section-placeholder";

export default function ProductsPage() {
  return (
    <SectionPlaceholder
      title="Products"
      note="Catalog list lands in PR4."
    />
  );
}
