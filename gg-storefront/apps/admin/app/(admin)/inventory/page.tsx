import { SectionPlaceholder } from "@/components/admin/section-placeholder";

export default function InventoryPage() {
  return (
    <SectionPlaceholder
      title="Inventory"
      note="Stock levels and low-stock alerts land in PR4."
    />
  );
}
